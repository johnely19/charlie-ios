import SwiftUI

extension Color {
    // Semantic colors that adapt to light/dark mode
    static let charlieBackground = Color("CharlieBackground")
    static let charlieCard = Color("CharlieCard")
    static let charlieAccent = Color("CharlieAccent")
}
